apiVersion: v1
kind: Service
metadata:
  name: topologist-scheduler
  namespace: topologist
spec:
  selector:
    app: topologist-scheduler
  ports:
    - port: 5001
      targetPort: 5001
  type: ClusterIP
