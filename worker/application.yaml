apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: topologist-worker
  namespace: devops-tools
spec:
  project: topologist
  source:
    repoURL: 'https://github.com/Nonsanxx/Topologist'
    targetRevision: HEAD
    path: worker/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: topologist
  syncPolicy:
    automated:
      prune: true
      selfHeal: true